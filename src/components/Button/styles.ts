import { StyleSheet } from 'react-native';

export const buttonStyle = StyleSheet.create({
  main: {
    flex: 1,
    backgroundColor: '#4245f5',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
